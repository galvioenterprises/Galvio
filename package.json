{
  "name": "galvio-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm build:images && pnpm build:index && ALLOW_SAMPLE_DATA=1 next dev",
    "build": "pnpm build:images && pnpm build:index && next build",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "preview": "wrangler dev",
    "deploy": "pnpm build && wrangler deploy",
    "import:products": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/import-products.mts",
    "build:preview": "pnpm build:images && pnpm build:index && ALLOW_SAMPLE_DATA=1 next build",
    "build:index": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/build-search-index.mts",
    "build:images": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/build-images.mts",
    "sync:voltas": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/fetch-voltas.mts",
    "match:voltas": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/match-voltas.mts",
    "bridge:voltas": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/bridge-voltas.mts",
    "sync:images": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/sync-images.mts",
    "admin": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/admin-server.mts"
  },
  "dependencies": {
    "next": "16.3.4",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^24.13.3",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "csv-parse": "^7.0.2",
    "eslint": "^9.39.5",
    "eslint-config-next": "16.3.4",
    "sharp": "^0.35.4",
    "tailwindcss": "^4",
    "typescript": "^5.9.3",
    "wrangler": "^4.129.0"
  },
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=24.0.0"
  }
}
